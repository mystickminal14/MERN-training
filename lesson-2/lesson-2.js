/**Datatypes refers  to the type of value variable will stored
 * There are two types of datatypes in javascript
 * They are : 
 * i) Primitive Datatype : Primitive datatype refers to datatypes which are built in and already predefined
 * ii) Non-primitive Datatype: Non primitive datatypes can be manipulated by users
 *-------------------P R I M I T I V E *** D A T A T Y P E S------------------------------------------------
 *Number : var num=10
 *String : let str="MINAL"
 *Boolean : const bool=true
 *BigInt : const bul= BigInt(4)
 *null :let bb=null
 *undefined:  let un  ----nothing is stored in un variable so it returns undefined
 *symbol : let sym= Symbol("this is stupid")
 *
 --------------------------N O N P R I M I T I V E  D A T A T Y P E-----------------------------------------
 * OBJECTS ARE NON PRIMITIVE DATATYPE
 * const is a very good way to declare objects
 * const obj ={
      name: "minal",
      sec :19,
      class :'A'
      handsome: true
 }
here name is item, and right hand side, its value is defined. similarly each item is represented by key which starts from 0.
---to access item of obj
console.log(obj)--prints object
console.log(obj['name'])-prints name item
console.log(obj.sec)-prints section
similarly, to add objects
we write:

      obj.fullname="minal pariyar"
      then 
      console.log(obj)
 * 
 * 
*/