// Runtime environment
//BROWSER RUNTIME---NODE  IS RUNTIME ENVIRONMENT FOR JAVSCRIPT
//console.log

/**V A R I A B L E
 * container of data or values
 * 1. var
 * 2. let
 * 3. const
 * var can be declared multiple number of times and it is updating the previous value
 * let can also  be  updated but it cannot be redeclared
 * const-it cannot be updated neither can be decalared
 */
var name='Minal'
let age=16
const address="Kathmandu"
var number=9861662986
let college="PCPS COLLEGE"
const uuid=2214714

const details= `|-------- M Y - P R O F I L E ----------- |\n| ---------- Full name : ${name} ---------- | \n| -------------- Age : ${age} --------------- | \n| --------- Address : ${address} --------- | \n| ------- Phone Number :${number} ------ |\n| ------- Address : ${college} -------- |\n| ------- University Id : ${uuid} --------|\n`
console.log(details)